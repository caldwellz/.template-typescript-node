{
  "name": "",
  "version": "0.0.1",
  "description": "",
  "author": "Zach Caldwell",
  "license": "MPL-2.0",
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --watch",
    "start": "npm run build && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^4.5.5"
  }
}
